:ruby
  rel = if page.next?
          'next'
        else
          page.prev? ? 'prev' : nil
        end
- if page.current?
  %li.page-item.active
    = content_tag :a, page, remote: remote, rel: rel, class: 'page-link'
- else
  %li.page-item
    = link_to page, url, remote: remote, rel: rel, class: 'page-link'
